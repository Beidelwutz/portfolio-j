---
const { name = "Jenny", headline, subtitle } = Astro.props;
---
<div class="hero">
  <div class="hero__visual reveal" data-reveal>
    <div class="hero__image-wrap">
      <img
        src="/uploads/vorstellung/Portfolio_01_Jenny-low.webp"
        alt="Jenny – Portfolio"
        width="600"
        height="400"
        fetchpriority="high"
      />
      <span class="hero__badge">das bin ich</span>
    </div>
  </div>

  <div class="hero__content">
    <div class="hero__intro reveal" data-reveal>
      <span class="hero__greeting">Hi, ich bin {name}</span>
    </div>
    <h1 class="hero__headline reveal" data-reveal>
      {headline ?? "Von der Idee zum fertigen Ergebnis"}
    </h1>
    <p class="hero__sub reveal" data-reveal>
      {subtitle ?? "Ich gestalte klare, moderne Web-Erlebnisse, die wirken und sich richtig gut anfühlen."}
    </p>
    <div class="hero__actions reveal" data-reveal>
      <a class="hero__cta" href="#kontakt">
        Lass uns reden
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
          <path d="M5 12h14M12 5l7 7-7 7"/>
        </svg>
      </a>
      <a class="hero__link" href="#videos">Portfolio ansehen</a>
    </div>
  </div>

  <a class="hero__scroll" href="#videos" aria-label="Weiter scrollen">
    <span>Entdecken</span>
    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
      <path d="M12 5v14M5 12l7 7 7-7"/>
    </svg>
  </a>
</div>
