---
const { theme = "light", id, className = "" } = Astro.props;
---
<section id={id} class={`section ${className}`} data-theme={theme}>
  <div class="container">
    <div class="section-surface">
      <slot />
    </div>
  </div>
</section>
